[project]
name = "scraper"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "anyio>=4.13.0",
    "browserforge>=1.2.4",
    "curl-cffi>=0.14.0",
    "playwright>=1.58.0",
    "scrapling[all]>=0.4.3",
]
